cmake_minimum_required (VERSION 2.6)
project (TBSE)

set (TBSE_VERSION_MAJOR 0)
set (TBSE_VERSION_MINOR 0)

FIND_PACKAGE(GIT)
FIND_PACKAGE(BOOST REQUIRED)
FIND_PACKAGE(QT4 REQUIRED)

add_executable(TGBE main.cpp)
